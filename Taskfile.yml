version: '3'

vars:
  APP_NAME: "Zashiki"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.PACKAGE_MANAGER | default "bun"}}'
  VITE_PORT: '{{.WAILS_VITE_PORT | default 9245}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml
  ios: ./build/ios/Taskfile.yml
  android: ./build/android/Taskfile.yml

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: "{{OS}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{OS}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{OS}}:run"

  install:
    summary: Builds and installs a cross-platform zashiki launcher into the user PATH
    desc: |
      Delegates to the current OS. macOS/Linux install a non-blocking `zashiki`
      shim into $HOME/.local/bin; Windows installs a detached launcher into
      %LOCALAPPDATA%\zashiki\launcher and adds it to the user PATH. All are per-user
      (no sudo/admin).
    cmds:
      - task: "{{OS}}:install"

  uninstall:
    summary: Removes the zashiki launcher from the user PATH
    cmds:
      - task: "{{OS}}:uninstall"

  dev:
    summary: Runs the application in development mode
    cmds:
      - wails3 dev -config ./build/config.yml -port {{.VITE_PORT}}

  generate:icons:
    summary: Generates all platform icons from the SVG source
    cmds:
      - task: common:generate:icons
    ignore_error: true

  setup:docker:
    summary: Builds Docker image for cross-compilation (~800MB download)
    cmds:
      - task: common:setup:docker

  build:server:
    summary: Builds the application in server mode (no GUI, HTTP server only)
    cmds:
      - task: common:build:server

  run:server:
    summary: Runs the application in server mode
    cmds:
      - task: common:run:server

  build:docker:
    summary: Builds a Docker image for server mode deployment
    cmds:
      - task: common:build:docker

  run:docker:
    summary: Builds and runs the Docker image
    cmds:
      - task: common:run:docker
